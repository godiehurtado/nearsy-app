/**
 * TypeScript module entry for `import '…/authService'`.
 * Metro still prefers `authService.android.ts` / `authService.ios.ts` at bundle time when present.
 */
export * from './authService.ios';
