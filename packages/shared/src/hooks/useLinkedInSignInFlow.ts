/**
 * Non-iOS stub — LinkedIn A3 sign-in is iOS Development-only in this package.
 */
export function useLinkedInSignInFlow() {
  return {
    signInWithLinkedIn: async () => {},
    linkedInSubmitting: false,
  };
}
