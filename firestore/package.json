{
  "name": "nearsy-firestore-rules",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-nearsy-rules \"node --test tests/users.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.4.0",
    "firebase-tools": "^14.18.0"
  }
}
