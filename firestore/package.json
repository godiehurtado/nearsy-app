{
  "name": "nearsy-firestore-rules",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-nearsy-rules \"node --test --test-concurrency=1 tests/users.rules.test.cjs tests/visibility-discovery.rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.4.0",
    "firebase-tools": "^14.18.0"
  }
}
